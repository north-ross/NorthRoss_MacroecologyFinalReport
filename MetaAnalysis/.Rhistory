# Get effect size
datES <- escalc(measure = "SMD", # I think this is best, since many of the studies use different units and measurement techniques
m2i = Control_mean,
sd2i = Control_SD,
n2i = Control_n,
m1i = Treatment_mean,
sd1i = Treatment_SD,
n1i = Treatment_n,
data = herbs)
# Get effect size
datES <- escalc(measure = "SMDH", # I think this is best,
# since many of the studies use different units and
# measurement techniques and different sample sizes
m2i = Control_mean,
sd2i = Control_SD,
n2i = Control_n,
m1i = Treatment_mean,
sd1i = Treatment_SD,
n1i = Treatment_n,
data = herbs)
grandmean1 <-rma.mv(
yi=yi,
V=vi,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
# Filter to just herbs
# Later I will compare the effect on herbs, trees and mosses
table(df$FG_Group)
library(orchaRd)
devtools::install_github("daniel1noble/orchaRd", force = TRUE)
install.packages("devtools")
devtools::install_github("daniel1noble/orchaRd", force = TRUE)
install.packages('bslib')
install.packages('bslib')
install.packages("devtools")
devtools::install_github("daniel1noble/orchaRd", force = TRUE)
# Grand mean
grandmean1 <-rma.mv(
yi=yi,
V=vi,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
# Try a one-variable with water table difference
wtd1 <-rma.mv(
yi=yi,
V=vi,
mods=~WTD_Difference,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
orchard_plot(wtd1,
mod = "WTD_Difference",
group = "PaperID",
xlab = "Standardised mean difference")
library(orchaRd)
library(orchaRd)
orchard_plot(wtd1,
mod = "WTD_Difference",
group = "PaperID",
xlab = "Standardised mean difference")
View(wtd1)
# Try a one-variable with Peatland type
wtd1 <-rma.mv(
yi=yi,
V=vi,
mods=~Peatland_type,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
orchard_plot(wtd1,
mod = "Peatland_type",
group = "PaperID",
xlab = "Standardised mean difference")
# Combine peatland types
table(df$Peatland_type)
df$Peatland_type <- replace(df$Peatland_type,
grepl(" fen", luc$Disturbance_Type),
"Fen")
df$Peatland_type <- replace(df$Peatland_type,
grepl(" fen", luc$Disturbance_Type),
"Fen")
df$Peatland_type <- replace(df$Peatland_type,
grepl(" fen", df$Peatland_type),
"Fen")
# Combine peatland types
table(df$Peatland_type)
orchard_plot(wtd1,
mod = "Peatland_type",
group = "PaperID",
xlab = "Standardised mean difference")
herbs <- subset(df,df$FG_Group=="Herb")
# Get effect size
datES <- escalc(measure = "SMDH", # I think this is best,
# since many of the studies use different units and
# measurement techniques and different sample sizes
m2i = Control_mean,
sd2i = Control_SD,
n2i = Control_n,
m1i = Treatment_mean,
sd1i = Treatment_SD,
n1i = Treatment_n,
data = herbs)
# Grand mean
grandmean1 <-rma.mv(
yi=yi,
V=vi,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
# Try a one-variable with Peatland type
wtd1 <-rma.mv(
yi=yi,
V=vi,
mods=~Peatland_type,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
orchard_plot(wtd1,
mod = "Peatland_type",
group = "PaperID",
xlab = "Standardised mean difference")
orchard_plot(wtd1,
main = "Effect of water table drawdown on herb production/abundance"
mod = "Peatland_type",
orchard_plot(wtd1,
main = "Effect of water table drawdown on herb production/abundance",
mod = "Peatland_type",
group = "PaperID",
xlab = "Standardised mean difference")
orchard_plot(wtd1,
main = "Effect of water table drawdown on herb production or abundance",
mod = "Peatland_type",
group = "PaperID",
xlab = "Standardised mean difference")
# Let's try doing all functional group data then separating them as a variable
fg <- subset(df, df$SR_or_FG=="FG")
# Get effect size
fgES <- escalc(measure = "SMDH", # I think this is best,
# since many of the studies use different units and
# measurement techniques and different sample sizes
m2i = Control_mean,
sd2i = Control_SD,
n2i = Control_n,
m1i = Treatment_mean,
sd1i = Treatment_SD,
n1i = Treatment_n,
data = fg)
# Try a one-variable with Peatland type
fg1 <-rma.mv(
yi=yi,
V=vi,
mods=~FG_Group,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
orchard_plot(fg1,
mod = "Peatland_type",
group = "PaperID",
xlab = "Standardised mean difference")
# Try a one-variable with Peatland type
fg1 <-rma.mv(
yi=yi,
V=vi,
mods=~FG_Group,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
df$FG_Group <- factor(df$FG_Group)
# Get effect size
fgES <- escalc(measure = "SMDH", # I think this is best,
# since many of the studies use different units and
# measurement techniques and different sample sizes
m2i = Control_mean,
sd2i = Control_SD,
n2i = Control_n,
m1i = Treatment_mean,
sd1i = Treatment_SD,
n1i = Treatment_n,
data = fg)
# Let's try doing all functional group data then separating them as a variable
fg <- subset(df, df$SR_or_FG=="FG")
# Get effect size
fgES <- escalc(measure = "SMDH", # I think this is best,
# since many of the studies use different units and
# measurement techniques and different sample sizes
m2i = Control_mean,
sd2i = Control_SD,
n2i = Control_n,
m1i = Treatment_mean,
sd1i = Treatment_SD,
n1i = Treatment_n,
data = fg)
# Try a one-variable with Peatland type
fg1 <-rma.mv(
yi=yi,
V=vi,
mods=~FG_Group,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
# Try a one-variable with FG
fg1 <-rma.mv(
yi=yi,
V=vi,
mods=~FG_Group,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
orchard_plot(fg1,
mod = "Peatland_type",
group = "PaperID",
xlab = "Standardised mean difference")
View(fg)
table(fg$FG_Group)
df$SR_or_FG=="FG"
# Let's try doing all functional group data then separating them as a variable
fg <- subset(df, df$SR_or_FG=="FG")
View(fg)
table(df$SR_or_FG)
df <- read.csv("ExtractedData_2026-05-12.csv")
# Convert all variances to SD
table(df$Biodiversity_Variance_unit)
df$Control_SD = df$Control_var
df$Treatment_SD = df$Treatment_var
for(i in 1:nrow(df)){
dfi <- df[i,]
if(dfi$Biodiversity_Variance_unit == "SE"){
dfi$Control_SD = dfi$Control_var * sqrt(dfi$Control_n)
dfi$Treatment_SD = dfi$Treatment_var * sqrt(dfi$Treatment_n)
}
df[i,] <- dfi
}
# Convert some variables to factors
df$Peatland_type <- factor(df$Peatland_type)
df$FG_Group <- factor(df$FG_Group)
# Combine peatland types
table(df$Peatland_type)
df$Peatland_type <- replace(df$Peatland_type,
grepl(" fen", df$Peatland_type),
"Fen")
# Filter to just herbs
# Later I will compare the effect on herbs, trees and mosses
table(df$FG_Group)
herbs <- subset(df,df$FG_Group=="Herb")
# Let's try doing all functional group data then separating them as a variable
fg <- subset(df, df$SR_or_FG=="FG")
# Get effect size
fgES <- escalc(measure = "SMDH", # I think this is best,
# since many of the studies use different units and
# measurement techniques and different sample sizes
m2i = Control_mean,
sd2i = Control_SD,
n2i = Control_n,
m1i = Treatment_mean,
sd1i = Treatment_SD,
n1i = Treatment_n,
data = fg)
# Try a one-variable with FG
fg1 <-rma.mv(
yi=yi,
V=vi,
mods=~FG_Group,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
View(fgES)
# Try a one-variable with FG
fg1 <-rma.mv(
yi=yi,
V=vi,
mods=FG_Group,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
# Try a one-variable with FG
fg1 <-rma.mv(
yi=yi,
V=vi,
mods=~FG_Group,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
# remove NA
fg <- fg[!is.na(fg$FG_Group), ]
# Let's try doing all functional group data then separating them as a variable
fg <- subset(df, df$SR_or_FG=="FG")
# Let's try doing all functional group data then separating them as a variable
fg <- subset(df, df$SR_or_FG=="FG")
# remove NA
fg <- fg[!is.na(fg$FG_Group), ]
table(fg$FG_Group)
fg$FG_Group <- replace(fg$FG_Group,
grepl(" moss", fg$FG_Group),
"Moss")
fg <- subset(fg, fg$FG_Group != "")
table(fg)
table(fg$FG_Group)
fg$FG_Group <- factor(fg$FG_Group)
# Get effect size
fgES <- escalc(measure = "SMDH", # I think this is best,
# since many of the studies use different units and
# measurement techniques and different sample sizes
m2i = Control_mean,
sd2i = Control_SD,
n2i = Control_n,
m1i = Treatment_mean,
sd1i = Treatment_SD,
n1i = Treatment_n,
data = fg)
# Try a one-variable with FG
fg1 <-rma.mv(
yi=yi,
V=vi,
mods=~FG_Group,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
# Try a one-variable with FG
fg1 <-rma.mv(
yi=yi,
V=vi,
mods=~FG_Group,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=fgES)
orchard_plot(fg1,
mod = "Peatland_type",
group = "PaperID",
xlab = "Standardised mean difference")
orchard_plot(fg1,
mod = "FG_Group",
group = "PaperID",
xlab = "Standardised mean difference")
orchard_plot(fg1,
mod = "FG_Group",
group = "PaperID",
xlab = "Standardised mean difference (with heteroscedastic population variances)")
# Filter to just herbs
# Later I will compare the effect on herbs, trees and mosses
table(df$FG_Group)
herbs <- subset(df,df$FG_Group=="Herb")
# Get effect size
datES <- escalc(measure = "SMDH", # I think this is best,
# since many of the studies use different units and
# measurement techniques and different sample sizes
m2i = Control_mean,
sd2i = Control_SD,
n2i = Control_n,
m1i = Treatment_mean,
sd1i = Treatment_SD,
n1i = Treatment_n,
data = herbs)
# Grand mean
grandmean1 <-rma.mv(
yi=yi,
V=vi,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
# Try a one-variable with Peatland type
peat1 <-rma.mv(
yi=yi,
V=vi,
mods=~Peatland_type,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
orchard_plot(peat1,
mod = "Peatland_type",
group = "PaperID",
xlab = "Standardised mean difference (with heteroscedastic population variances)")
df <- read.csv("ExtractedData_2026-05-12.csv")
# Convert all variances to SD
table(df$Biodiversity_Variance_unit)
df$Control_SD = df$Control_var
df$Treatment_SD = df$Treatment_var
for(i in 1:nrow(df)){
dfi <- df[i,]
if(dfi$Biodiversity_Variance_unit == "SE"){
dfi$Control_SD = dfi$Control_var * sqrt(dfi$Control_n)
dfi$Treatment_SD = dfi$Treatment_var * sqrt(dfi$Treatment_n)
}
df[i,] <- dfi
}
# Note -some of the variances are too high and need to be corrected
# Convert some variables to factors
df$Peatland_type <- factor(df$Peatland_type)
df$FG_Group <- factor(df$FG_Group)
# Combine peatland types
table(df$Peatland_type)
df$Peatland_type <- replace(df$Peatland_type,
grepl(" fen", df$Peatland_type),
"Fen")
table(df$Peatland_type)
df <- read.csv("ExtractedData_2026-05-12.csv")
# Convert all variances to SD
table(df$Biodiversity_Variance_unit)
df$Control_SD = df$Control_var
df$Treatment_SD = df$Treatment_var
for(i in 1:nrow(df)){
dfi <- df[i,]
if(dfi$Biodiversity_Variance_unit == "SE"){
dfi$Control_SD = dfi$Control_var * sqrt(dfi$Control_n)
dfi$Treatment_SD = dfi$Treatment_var * sqrt(dfi$Treatment_n)
}
df[i,] <- dfi
}
# Note -some of the variances are too high and need to be corrected
# Combine peatland types
df$Peatland_type <- replace(df$Peatland_type,
grepl(" fen", df$Peatland_type),
"Fen")
table(df$Peatland_type)
# Combine functional groups
df$FG_Group <- replace(df$FG_Group,
grepl(" moss", df$FG_Group),
"Moss")
# Remove empty
df <- subset(df, df$FG_Group != "")
# Convert some variables to factors
df$Peatland_type <- factor(df$Peatland_type)
df$FG_Group <- factor(df$FG_Group)
# Let's try doing all functional group data then separating them as a variable
fg <- subset(df, df$SR_or_FG=="FG")
# remove NA
fg <- fg$FG_Group
fg$FG_Group <- factor(fg$FG_Group)
# Let's try doing all functional group data then separating them as a variable
fg <- subset(df, df$SR_or_FG=="FG")
fg$FG_Group <- factor(fg$FG_Group)
# Get effect size
fgES <- escalc(measure = "SMDH", # I think this is best,
# since many of the studies use different units and
# measurement techniques and different sample sizes
m2i = Control_mean,
sd2i = Control_SD,
n2i = Control_n,
m1i = Treatment_mean,
sd1i = Treatment_SD,
n1i = Treatment_n,
data = fg)
# Try a one-variable with FG
fg1 <-rma.mv(
yi=yi,
V=vi,
mods=~FG_Group,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=fgES)
orchard_plot(fg1,
mod = "FG_Group",
group = "PaperID",
xlab = "Standardised mean difference (with heteroscedastic population variances)")
# I think this looks pretty good?
# Filter to just herbs
# Later I will compare the effect on herbs, trees and mosses
table(df$FG_Group)
herbs <- subset(df,df$FG_Group=="Herb")
# Get effect size
datES <- escalc(measure = "SMDH", # I think this is best,
# since many of the studies use different units and
# measurement techniques and different sample sizes
m2i = Control_mean,
sd2i = Control_SD,
n2i = Control_n,
m1i = Treatment_mean,
sd1i = Treatment_SD,
n1i = Treatment_n,
data = herbs)
# Grand mean
grandmean1 <-rma.mv(
yi=yi,
V=vi,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
# Try a one-variable with Peatland type
peat1 <-rma.mv(
yi=yi,
V=vi,
mods=~Peatland_type,
random= ~1|PaperID/ID,
method="REML",
digits=4,
data=datES)
orchard_plot(peat1,
mod = "Peatland_type",
group = "PaperID",
xlab = "Standardised mean difference (with heteroscedastic population variances)")
View(grandmean1)
summary(grandmean1)
